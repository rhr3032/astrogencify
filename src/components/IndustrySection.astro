---
import Button from "./Button.astro";
import type { Link } from "../types";

interface Industry {
  title: string;
  text: string;
  tags?: string[];
  link: Link;
}

interface Props {
  title: string;
  subtitle?: string;
  items: Industry[];
  cta?: Link;
}

const { title, subtitle, items, cta } = Astro.props;
---

<section class="sy-industries">
  <div class="u-container container">
    <div class="heading">
      <h2 class="u-heading title" data-sy-reveal="words">{title}</h2>
      {subtitle ? (
        <p class="u-text subtitle" data-sy-reveal="lines">{subtitle}</p>
      ) : null}
      {cta ? <Button {...cta} arrow class="cta" /> : null}
    </div>
    <div class="grid">
      {items.map((item) => (
        <article class="card glass-card">
          <div class="card-top">
            <h3 class="u-heading" data-sy-reveal="words">{item.title}</h3>
            {item.tags ? (
              <div class="tags">
                {item.tags.map((tag) => (
                  <span class="u-text">{tag}</span>
                ))}
              </div>
            ) : null}
          </div>
          <p class="u-text" data-sy-reveal="lines">{item.text}</p>
          <Button {...item.link} size="sm" color="primary-alt" />
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .sy-industries {
    margin: 8rem 0;
    position: relative;
    z-index: 2;
  }

  .container {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }

  .heading {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1rem;
  }

  .title {
    --fs: 2.8rem;
  }

  .subtitle {
    --fs: var(--font-size-md);
    color: var(--color-text-alt);
    max-width: 44em;
  }

  .cta {
    margin-top: 0.5rem;
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1.5rem;

    @media (--mq-tablet) {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    @media (--mq-phone) {
      grid-template-columns: 1fr;
    }
  }

  .card {
    padding: 2rem;
    border-radius: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    box-shadow: 0 12px 28px rgba(10, 20, 40, 0.08);
  }

  .card-top {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .card h3 {
    --fs: 1.4rem;
    --fw: 500;
  }

  .card p {
    color: var(--color-text-alt);
    flex: 1;
  }

  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .tags span {
    --fs: var(--font-size-xs);
    background: rgba(80, 100, 255, 0.1);
    color: var(--color-brand-1);
    padding: 0.35rem 0.75rem;
    border-radius: 1rem;
  }
</style>
