---
import Layout from "../../layouts/Layout.astro";
import HeroHome from "../../components/HeroHome.astro";
import Button from "../../components/Button.astro";
---

<Layout title="Meridian LMS">
  <HeroHome
    title="Meridian LMS"
    subtitle="A learning platform with cohort analytics and certification flows."
    cta={{ title: "Start a Project", url: "/contact" }}
  />

  <section class="case">
    <div class="u-container">
      <div class="intro">
        <h2 class="u-heading">Overview</h2>
        <p class="u-text">
          We redesigned the LMS experience to support blended learning, cohort
          reporting, and mobile-first access. The platform now supports thousands
          of learners with improved completion rates.
        </p>
      </div>
      <div class="grid">
        <div class="card glass-card">
          <h3 class="u-heading">Challenge</h3>
          <p class="u-text">
            Low completion rates and fragmented reporting for instructors.
          </p>
        </div>
        <div class="card glass-card">
          <h3 class="u-heading">Solution</h3>
          <p class="u-text">
            Cohort dashboards, improved content flow, and mobile-first UX.
          </p>
        </div>
        <div class="card glass-card">
          <h3 class="u-heading">Results</h3>
          <ul class="u-text">
            <li>150k active learners</li>
            <li>35 percent higher completion</li>
            <li>New certification workflow</li>
          </ul>
        </div>
      </div>
      <div class="stack">
        <h3 class="u-heading">Tech stack</h3>
        <ul class="u-text">
          <li>React, TypeScript</li>
          <li>Node.js, PostgreSQL</li>
          <li>Video streaming integrations</li>
        </ul>
      </div>
    </div>
  </section>

  <section class="cta">
    <div class="u-container">
      <h2 class="u-heading">Plan your LMS roadmap</h2>
      <Button title="Talk to us" url="/contact" arrow />
    </div>
  </section>
</Layout>

<style>
  .case {
    margin: 6rem 0;
  }

  .intro {
    max-width: 44rem;
    margin-bottom: 2.5rem;
  }

  .intro h2 {
    --fs: 2.4rem;
  }

  .intro p {
    color: var(--color-text-alt);
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1.5rem;

    @media (--mq-tablet) {
      grid-template-columns: 1fr;
    }
  }

  .card {
    padding: 2rem;
    border-radius: 1.5rem;
    background: transparent;
    border: none;
    box-shadow: 0 12px 28px rgba(10, 20, 40, 0.08);
  }

  .card h3 {
    --fs: 1.2rem;
    --fw: 500;
    margin-bottom: 1rem;
  }

  .card p,
  .card ul {
    color: var(--color-text-alt);
  }

  .card ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 0.5rem;
  }

  .stack {
    margin-top: 2.5rem;
  }

  .stack h3 {
    --fs: 1.3rem;
    margin-bottom: 0.75rem;
  }

  .stack ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 0.4rem;
    color: var(--color-text-alt);
  }

  .cta {
    margin: 4rem 0 8rem;
  }

  .cta h2 {
    --fs: 2.2rem;
    margin-bottom: 1rem;
  }
</style>
