---
import Layout from "../../layouts/Layout.astro";
import HeroHome from "../../components/HeroHome.astro";
import Button from "../../components/Button.astro";
---

<Layout title="Pulse Mobile Banking">
  <HeroHome
    title="Pulse Mobile Banking"
    subtitle="Secure mobile banking with fast onboarding and wallet management."
    cta={{ title: "Start a Project", url: "/contact" }}
  />

  <section class="case">
    <div class="u-container">
      <div class="intro">
        <h2 class="u-heading">Overview</h2>
        <p class="u-text">
          We delivered a mobile banking app with secure onboarding, biometric
          login, and real-time transaction insights. The app improved conversion
          and reduced onboarding drop-off.
        </p>
      </div>
      <div class="grid">
        <div class="card glass-card">
          <h3 class="u-heading">Challenge</h3>
          <p class="u-text">
            Complex onboarding and low completion rates for new users.
          </p>
        </div>
        <div class="card glass-card">
          <h3 class="u-heading">Solution</h3>
          <p class="u-text">
            Streamlined onboarding with guided verification and smart defaults.
          </p>
        </div>
        <div class="card glass-card">
          <h3 class="u-heading">Results</h3>
          <ul class="u-text">
            <li>75 percent onboarding completion</li>
            <li>Higher app store ratings</li>
            <li>Reduced support tickets</li>
          </ul>
        </div>
      </div>
      <div class="stack">
        <h3 class="u-heading">Tech stack</h3>
        <ul class="u-text">
          <li>Swift and Kotlin</li>
          <li>Secure auth and biometrics</li>
          <li>Real-time data sync</li>
        </ul>
      </div>
    </div>
  </section>

  <section class="cta">
    <div class="u-container">
      <h2 class="u-heading">Build a mobile finance product</h2>
      <Button title="Talk to us" url="/contact" arrow />
    </div>
  </section>
</Layout>

<style>
  .case {
    margin: 6rem 0;
  }

  .intro {
    max-width: 44rem;
    margin-bottom: 2.5rem;
  }

  .intro h2 {
    --fs: 2.4rem;
  }

  .intro p {
    color: var(--color-text-alt);
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1.5rem;

    @media (--mq-tablet) {
      grid-template-columns: 1fr;
    }
  }

  .card {
    padding: 2rem;
    border-radius: 1.5rem;
    background: transparent;
    border: none;
    box-shadow: 0 12px 28px rgba(10, 20, 40, 0.08);
  }

  .card h3 {
    --fs: 1.2rem;
    --fw: 500;
    margin-bottom: 1rem;
  }

  .card p,
  .card ul {
    color: var(--color-text-alt);
  }

  .card ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 0.5rem;
  }

  .stack {
    margin-top: 2.5rem;
  }

  .stack h3 {
    --fs: 1.3rem;
    margin-bottom: 0.75rem;
  }

  .stack ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 0.4rem;
    color: var(--color-text-alt);
  }

  .cta {
    margin: 4rem 0 8rem;
  }

  .cta h2 {
    --fs: 2.2rem;
    margin-bottom: 1rem;
  }
</style>
